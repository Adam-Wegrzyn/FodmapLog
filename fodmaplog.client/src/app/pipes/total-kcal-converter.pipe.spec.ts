import { TotalKcalConverterPipe } from '../total-kcal-converter.pipe';

describe('TotalKcalConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalKcalConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
